rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // السماح بالقراءة والكتابة للجميع أثناء مرحلة التطوير
    match /{document=**} {
      allow read, write: if true;
    }
  }
}